class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        j = 0
        for num in nums:
            if(nums != 0):

                nums[j] = num
                ? +_ 1

        ___ x __ r.. ? l.. ?
            ? ? _ 0
