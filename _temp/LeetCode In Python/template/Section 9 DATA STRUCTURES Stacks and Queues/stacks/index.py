c_ PlateStack:

    ___ -  
        st _    # list

    ___ push x: int) -> N..:
        st.a..(x)

    ___ pop ) -> N..:
        __(l..(st) > 0
            st.pop()

    ___ top ) -> int:
        __(l..(st) __ 0
            r_ N..
        r_ st[-1]

    ___ getLen ) -> int:
      r_ l..(st)